# Run primary_phenotypes_retriever.sh to download the html of each study from dbGaP web page and retrieve the primary phenotype annotations (it's parallelized in 4 cores but it lasts quite some time to fininsh)

# Use the notebook dbGaP primary phenotypes.ipynb to plot the results
